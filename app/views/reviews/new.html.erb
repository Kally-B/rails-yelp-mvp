<%= render "restaurants/navbar" %>

<div class="container">
  <h1>Add a review to <%= @restaurant.name %></h1>

  <%= render "form", restaurant: @restaurant, review: @review %>

<%= link_to "Back", restaurant_path(@restaurant), class: "btn btn-primary mt-4" %>

</div>
